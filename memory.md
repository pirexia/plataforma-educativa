# memory.md — Estado del proyecto

> Fichero de estado entre sesiones. Claude lo lee al arrancar y lo actualiza tras cada hito.
> Mantenerlo **corto**: si supera unas 150 líneas, archiva lo antiguo en `docs/historial/`.

---

## Estado actual

**Fase**: 0 — Cimientos
**Paso activo**: 0.1-0.9 y 0.13 cerrados y mezclados a `develop`. Siguiente: **0.10 · Decidir alojamiento del piloto** y el resto de la serie 0.10b-0.10e — bloqueantes no técnicos (`OPEN-11`/`OPEN-08`/`OPEN-09`/`OPEN-10`), ver `Siguiente paso concreto`.
**Rama**: PR de 0.9 (`feature/REQ-0.9-auditoria-i18n`) pendiente de confirmar que se fusionó a `develop` al arrancar la próxima sesión — esta sesión se cerró justo tras abrirlo. Si ya está fusionado, sincronizar y borrar la rama local/remota.
**Última sesión (2026-08-18)**: cerrado el paso 0.9 (auditoría e i18n transversales). Antes de empezarlo, se detectó que faltaba `ADR-035` (pedido en una sesión anterior y nunca escrito): resuelve `OPEN-12` — qué hacer con los identificadores personales de `audit_logs.changes` frente al derecho de supresión (decisión: no se escribe su valor desde el origen; la supresión se completa por retención, no por edición de la fila inmutable). Redactado por `architect` (Opus), revisado por `doc-reviewer` (2 hallazgos Media corregidos: `ADR-034` se había editado en vez de sustituirse — revertido —, y `PRIVACY.md` quedaba contradicho), mezclado vía PR [#26](https://github.com/pirexia/plataforma-educativa/pull/26). Con eso desbloqueado, 0.9 implementado por un subagente `fork` en *worktree* aislado (observer de auditoría según `ADR-035`, sistema de i18n de 4 idiomas con `vue-i18n`, detector de literales `INV-009`). Revisión independiente real de `security-reviewer`/`doc-reviewer` encontró 1 hallazgo Alta (`Tenant` fuera del mecanismo de auditoría pese a que `ADR-035 §8` lo declaraba en alcance, sin ADR sustitutivo) y varios Media, todos corregidos en la misma sesión por la orquestadora directamente (sin delegar): `ADR-036` nuevo (sustituye esa fila de `ADR-035 §8`, issue de seguimiento [#27](https://github.com/pirexia/plataforma-educativa/issues/27) para 1.6), guarda en `AuditRecorder`, envoltorio propio de `vue-i18n` (`RNF-MANT-007`), `docs/i18n.md` nuevo, `docs/modulos/REQ-CORE/datos.md` completado. 108 tests en verde. Detalle completo en `docs/historial/0.9-auditoria-i18n.md`. PR de 0.9 abierto, **sin confirmar fusión** — la sesión se cortó por cuota justo después de abrirlo, ver `Siguiente paso concreto`.

---

## Decisiones tomadas

| ADR | Decisión |
|-----|----------|
| ADR-001 | Multi-tenant: BD compartida con `tenant_id` + RLS |
| ADR-002 | Monolito modular, no microservicios |
| ADR-004 | Borrado: lógico / anonimización / purga, en tres niveles |
| ADR-007 | Stack: Laravel + Vue 3 TS + PostgreSQL |
| ADR-008 | Móvil: PWA → Capacitor |
| ADR-015 | Segmento: concertados de Madrid, objetivo Colegio Miramadrid |
| ADR-016 | Complementarios a Raíces/Roble, no sustitutos. Excepción: 0-3 privado |
| ADR-020 | Régimen jurídico por etapa, no por tenant |
| ADR-021 | Idiomas: es-ES, en, de, fr |
| ADR-023 | UI: Tailwind + shadcn-vue + TanStack Table |
| ADR-024 | Infra: Compose sobre VPS → Kubernetes en E2 |
| ADR-025 | Auth SPA: cookie de sesión, prohibido JWT en localStorage |
| ADR-026 | Documentación híbrida: raíz + por módulo |
| ADR-027 | Podman sobre RHEL 10 en VM VMware; Quadlet en producción. Sustituido para desarrollo (E0) por ADR-030 |
| ADR-028 | Topología de red: frontend sin proxy, red externa, `Wants=` no `Requires=` |
| ADR-029 | `public_id` ULID en API y URLs; `TIMESTAMPTZ`, `text`, céntimos enteros |
| ADR-030 | Desarrollo en WSL2, equipo personal, **solo datos sintéticos** |
| ADR-031 | `REQ-TRAN` ampliado a 12 requisitos, SHOULD, fase 2 |
| ADR-032 | Lista maestra única de autorizados a recoger, en `REQ-FAM-UNIT-005` |
| ADR-035 | `audit_logs.changes` no registra valores identificativos; supresión por retención, no por edición (resuelve `OPEN-12`) |
| ADR-036 | `Tenant` fuera del *observer* de auditoría de tenant; su ciclo de vida se audita en `admin_action_logs` (paso 1.6) |

---

## Trabajo en curso

- **0.1 cerrado**: `LICENSE` propietaria (titular provisional: Andrés Matías López, pendiente de `OPEN-07`), `.gitignore` con patrones de Python, eliminado `SKILL.md` suelto de la raíz.
- **0.2 cerrado**: MCP de GitHub y Context7 conectados y verificados con las 4 pruebas de `docs/SETUP-ENTORNO.md` §7.4 (issue de prueba #1 creado y cerrado). Plugin `laravel/agent-skills` instalado. 9 agentes con modelo correcto, 10 skills.
- **0.3 cerrado**: Podman, red externa `plataforma-net` y `.wslconfig` ya estaban operativos de una sesión previa. `compose.yaml` con perfil reducido: `postgres` + `redis` + `api` + `web` arrancan por defecto y probados en `healthy`; `minio` detrás de `--profile full`. `SYSADMIN.md` v0.3.0. Pendiente a propósito: servicio de PDF (motor sin decidir, ver 1.17).
- **0.4 cerrado**: Laravel 13 en `apps/api` (PHP 8.4). `app/Modules/<Modulo>/{Domain,Application,Infrastructure,Http}` con autodescubrimiento de `ServiceProvider` (`App\Support\Modules\ModuleServiceProviderDiscovery`), sin módulos reales todavía. `GET /api/health` documentado en `openapi.yaml`. Pest (4 tests) y Larastan nivel 6 en verde. Contenedorizado (`infra/containers/api/Containerfile`), conexión a PostgreSQL verificada desde dentro del contenedor. `routes/web.php` vaciado y `resources/views/welcome.blade.php` eliminada: backend puramente API (`INV-006`), la SPA de `apps/web` es el único cliente web.
- **Bug propio detectado y corregido (0.4)**: el `Containerfile` inicial purgaba `libpq-dev`/`libzip-dev` con `--auto-remove` tras compilar las extensiones, lo que también se llevaba `libpq.so.5`/`libzip.so.4` (dependencias en tiempo de ejecución) y dejaba `pdo_pgsql`/`zip` sin cargar. El healthcheck no lo detectó porque no toca la base de datos — lo encontré al probar `php artisan db:show` dentro del contenedor. Corregido no purgando las `-dev` (imagen de desarrollo, no de producción).
- **0.5 cerrado**: Vue 3 + TS + Vite en `apps/web` (`npm create vite`, no `create-vue`: su instalador interactivo se colgó consumiendo CPU al 100% con `yes ""` — proceso matado a mano, ver P-02). Tailwind v4 + shadcn-vue inicializados con `--defaults --yes`; quitado el `@import` a Google Fonts que trae la plantilla por defecto (llamada a un tercero desde cada carga, cuestión de privacidad con un producto que trata datos de menores). `vue-router`, `AppLayout` + `HomeView` que consume `GET /api/health` de verdad. Cliente API propio en `src/api/client.ts` (fetch nativo, sin librería), con `credentials: 'include'` ya previsto para la cookie de sesión de `ADR-025`. ESLint (flat config) + Prettier, Vitest (4 tests) y Playwright (1 e2e) en verde. Contenedorizado igual que `api` (`infra/containers/web/Containerfile`, mismo `node_modules` del host montado por volumen).
- **MCP añadidos**: Laravel Boost (`composer require laravel/boost --dev` + `php artisan boost:install --mcp --no-interaction`) y Playwright (`@playwright/mcp`), declarados en `.mcp.json` de la raíz (versionado, sin secretos). PostgreSQL MCP pendiente de 0.8.
- **Bug propio detectado y corregido (sesión nueva, post-0.5)**: `laravel-boost` seguía sin conectar (`-32000: Connection closed`) al abrir la sesión nueva prevista para cargarlo. La ruta relativa `apps/api/artisan` en `.mcp.json` solo resuelve si el proceso de Claude Code arranca con cwd en la raíz del repo; esta sesión abrió en `docs/`, así que PHP fallaba con `Could not open input file`. `${CLAUDE_PROJECT_DIR}` no está disponible para expansión en `.mcp.json` en la versión instalada (2.1.231, confirmado por el propio diagnóstico de `claude mcp list`). Corregido envolviendo el comando en `sh -c 'cd "$(git rev-parse --show-toplevel)" && exec php apps/api/artisan boost:mcp'`, portable y sin rutas absolutas de un usuario concreto. Documentado en issue #2 (cerrado), commit `548df67` mergeado a `develop` en `c1160a6`. Verificado con `claude mcp list`: los seis MCP (GitHub, Context7, Playwright, Laravel Boost, MyInvestor, Google Drive) conectan.
- **P-01 posiblemente resuelto**: en esta sesión `spec-writer` sí aparece en la lista de subagentes disponibles. Pendiente confirmar en una sesión futura que no fue un efecto puntual antes de cerrar el problema abierto.
- **0.6 cerrado** (PR #4 mergeado a `develop` en `aa34d35`): `ci-api.yml` (Pest/Pint/Larastan sobre PHP 8.4, runner nativo, sin contenedor), `ci-web.yml` (ESLint/vue-tsc+build/Vitest/Playwright), `dependency-scan.yml` (Trivy filesystem, sin subir SARIF). Corregido de paso `apps/api/composer.json` (`^8.3` → `^8.4`, issue #3, **cerrado el 2026-08-18**: el código se corrigió en esta sesión pero el issue se quedó abierto por descuido, detectado y corregido al arrancar 0.8). `renovate.json` listo, App sin instalar todavía. Dos bugs encontrados y corregidos en el propio proceso: (1) el e2e de Playwright fallaba en el runner con timeout de `webServer` porque Vite escuchaba en `localhost` sin `--host` explícito (ambigüedad IPv4/IPv6 entre entornos) — fijado `--host 127.0.0.1` en `playwright.config.ts`; (2) `actions/dependency-review-action` no es viable en este repo (privado, sin GitHub Advanced Security disponible en cuenta personal) — sustituido por Trivy. Además, error propio de proceso: un `rm` fuera de `Edit`/`Write` se me olvidó stagear (`git add` solo cubrió los ficheros que creía haber tocado), dejando el workflow viejo trackeado y ejecutándose igualmente — corregido revisando `git status` completo. **Pendiente**: activar *branch protection* en GitHub con los ocho checks (3 de `ci-api.yml` + 4 de `ci-web.yml` + 1 de `dependency-scan.yml`) como *required status checks* (bloqueo de merge real, no automatizable desde Claude Code) — ver `SYSADMIN.md` §4.
- **Bug propio detectado y corregido (post-0.6, previo a activar branch protection)**: `ci-api.yml` y `ci-web.yml` tenían filtro `paths:` (solo se disparaban si el PR tocaba `apps/api/**`/`apps/web/**`). Si esos checks se marcan como *required* en branch protection, cualquier PR que no toque esas rutas (p.ej. uno que solo cambia `memory.md`) nunca los dispara y GitHub bloquea el merge para siempre ("Expected — waiting for status to be reported"). Corregido quitando los filtros `paths:`: ambos workflows corren siempre. Detectado antes de configurar branch protection, no en producción.
- **0.7 cerrado** (núcleo multi-tenant, paso crítico; PR #5 y #9 mezclados a `develop` en `dc15584`/`caaf84e`; ramas borradas). Diseño en `docs/adr/ADR-033-implementacion-del-aislamiento-multi-tenant.md`; RLS de PostgreSQL como barrera primaria, scope de Eloquent (`TenantModel`/`BelongsToTenant`/`TenantScope`) como ergonomía secundaria; tres roles PostgreSQL sin `SUPERUSER`; middleware `ResolveTenant` por subdominio; colas, caché y almacenamiento conscientes de tenant; helper `TenantMigration::tenantTable()` para tablas de negocio futuras. 47 tests en `tests/Feature/Tenancy/`, estable en corridas repetidas. `db-reviewer` y `security-reviewer` corrieron antes de mezclar (obligatorio, `CLAUDE.md` §6): sin bloqueantes; 1 hallazgo Alta y 2 Media del `db-reviewer`, todos corregidos; `security-reviewer` abrió 3 issues de seguimiento no bloqueantes ([#6](https://github.com/pirexia/plataforma-educativa/issues/6), [#7](https://github.com/pirexia/plataforma-educativa/issues/7), [#8](https://github.com/pirexia/plataforma-educativa/issues/8), ver `Problemas abiertos`). **Detalle completo (checklist de los 11 subpasos, los 7 bugs propios encontrados, y el porqué de cada decisión) archivado en `docs/historial/0.7-nucleo-multitenant.md`** — consultar ahí antes de tocar `app/Support/Tenancy/` en 0.8, no reconstruir el razonamiento desde cero.
- **Problema de entorno detectado durante 0.7 (no del repositorio, sigue sin resolver)**: `/auto-mode-setup` añadió a `.claude/settings.json` la regla `"deny": ["Read(./.env.*)", ...]`. El patrón `./.env.*` es más amplio de lo que probablemente se pretendía: bloquea también `.env.example`/`apps/api/.env.example` (plantillas sin secretos, sí deben poder leerse y editarse) además de los `.env` reales (correcto que estén bloqueados). Avisar al usuario si una sesión futura necesita tocar un `.env.example` y se topa con esto.
- **0.8 cerrado** (modelo de datos núcleo; PR [#21](https://github.com/pirexia/plataforma-educativa/pull/21) mezclada a `develop`, squash, rama borrada). Diseño en `docs/adr/ADR-034-modelo-de-datos-nucleo.md`: `Person`/`User` separados (identidad vs. credencial); esquema completo de `Role`/`Permission` desde ahora, resolutor granular diferido a 1.5; `AuditLog` polimórfica append-only con `REVOKE UPDATE, DELETE` en el motor para los dos roles de aplicación; `AcademicYear` con `academic_year_id` obligatorio-o-ausente, nunca nullable; `ModuleSubscription` con catálogo materializado desde el código vía `platform:sync-registry`. `TenantModel` gana `SoftDeletes`+`RecordsAuthorship` (corrige pérdida de datos silenciosa: `deleted_at` existía desde 0.7 pero no se usaba). 94 tests en verde. **Proceso de dos fases**: la implementación (12 subpasos) corrió como subagente `fork` en *worktree* aislado — un *fork* no puede invocar la herramienta Agent, así que se autoaplicó los checklists de `db-reviewer`/`security-reviewer` en vez de invocarlos de verdad (encontró y corrigió 1 Alta [#17] y 1 Media [#16] por su cuenta). La sesión orquestadora relanzó ambos revisores **de verdad**, como subagentes independientes, con instrucción explícita de no dar por buenos esos arreglos sin comprobarlos: los confirmó correctos y encontró 2 hallazgos Media más ([#19](https://github.com/pirexia/plataforma-educativa/issues/19)/[#20](https://github.com/pirexia/plataforma-educativa/issues/20), corregidos en la misma sesión) y 1 diferido a propósito a 1.2 (`#18`, ver `Problemas abiertos`). **Lección de proceso**: cuando la implementación de un paso la hace un *fork*, la revisión de `db-reviewer`/`security-reviewer` que exige `CLAUDE.md` §6 antes de mezclar tiene que lanzarla la sesión orquestadora aparte — la autorrevisión de un *fork* no sustituye la revisión independiente, por buena que sea. Detalle completo (12 subpasos, 6 bugs propios, ambas rondas de revisión) en `docs/historial/0.8-modelo-de-datos-nucleo.md`.
- **Incidente de proceso durante 0.8, ya resuelto**: el *fork* de implementación exportaba `APP_KEY`/credenciales de PostgreSQL a mano en cada comando en vez de dejarlas en `apps/api/.env` (un *worktree* nuevo no lo trae, está en `.gitignore`) — como el estado del shell no persiste entre llamadas de Bash en este entorno, esto obligaba a repetir el mismo permiso una y otra vez en el móvil del usuario. Corregido copiando el `.env` real al *worktree*. De paso apareció una fuga peor: alguien había guardado esa misma orden de `export` con las credenciales **en texto plano** como entrada literal de `allow` en `.claude/settings.local.json` (fichero personal, no versionado, riesgo bajo por ser solo credenciales de desarrollo — pero mala práctica). Eliminada esa entrada y sustituida por `Bash(php artisan migrate:*)` en el `allow` local (con `migrate:fresh`/`db:wipe` bloqueados siempre en el `deny` compartido). Si una sesión futura ve prompts de permiso idénticos repitiéndose muchas veces, es señal de este mismo patrón (variable exportada en vez de persistida) — parar y revisar el `.env`, no seguir aprobando.
- **`ADR-035` cerrado** (PR [#26](https://github.com/pirexia/plataforma-educativa/pull/26), squash a `develop`). Resuelve `OPEN-12`. Decisión: el derecho de supresión no se ejerce dentro de `audit_logs` (inmutable sin excepciones); se evita que entre cualquier valor identificativo desde el origen (interfaz `Auditable`, políticas `Full`/`Selective`/`Redacted` por modelo, lista de inclusión con fallo en cerrado) y la supresión se completa por vencimiento del plazo de retención. Descarta *crypto-shredding* (choca con `REQ-BKP-005`: restaurar una copia restauraría la clave) y redacción dirigida (circular, no acota la búsqueda). `doc-reviewer` encontró y se corrigió: `ADR-034` se había editado en vez de sustituirse por un ADR nuevo (revertido, `docs/adr/README.md` exige inmutabilidad) y `PRIVACY.md` quedaba contradiciendo el cierre (actualizado).
- **0.9 cerrado** (auditoría e i18n transversales; PR de la rama `feature/REQ-0.9-auditoria-i18n`, ver arriba si sigue sin confirmar fusión). Observer de auditoría según `ADR-035` (interfaz `Auditable`, `AuditChangeBuilder` con los 5 pasos de evaluación, *observer* de ciclo de vida, 5 modelos de tenant declarados) e i18n de 4 idiomas (`vue-i18n`, envuelto tras `useT()` por `RNF-MANT-007`; detector de literales `INV-009` enganchado a `ci-web.yml`). Implementado por un *fork* en *worktree* aislado (10 commits), con autorrevisión que no sustituye la revisión independiente real — otra vez, como en 0.8. `security-reviewer`/`doc-reviewer` lanzados de verdad por la orquestadora encontraron 1 Alta (`Tenant` fuera del alcance que `ADR-035 §8` declaraba, sin ADR sustitutivo) y varias Media, corregidas todas en la misma sesión sin delegar más: `ADR-036` (nuevo, sustituye esa fila de `§8`, issue [#27](https://github.com/pirexia/plataforma-educativa/issues/27) de seguimiento a 1.6), guarda en `AuditRecorder` para `isPlatformMode()`, envoltorio de `vue-i18n`, `docs/i18n.md`, `docs/modulos/REQ-CORE/datos.md` completado. 108 tests en verde, Pint/Larastan/ESLint/`vue-tsc`/`lint:i18n` limpios. Detalle completo (los 2 bugs propios del *fork*, ambas rondas de revisión) en `docs/historial/0.9-auditoria-i18n.md`.

---

## Bloqueantes

| ID | Descripción | Impacto |
|----|-------------|---------|
| H0 | Sin centro piloto comprometido ni ficheros de exportación de GQdalya | Criterio de salida de fase 1 y `REQ-ONB-003` |
| OPEN-11 | **Dónde se aloja el piloto**. WSL2 no puede alojar datos reales | Hito H0 |
| OPEN-07 | Entidad jurídica y contrato de encargado de tratamiento | Datos reales y facturación |
| OPEN-08 | Dominio y DNS con API para certificado comodín | Multi-tenant, fase 0 |
| OPEN-09 | Proveedor de correo transaccional | `REQ-AUTH`, `REQ-COM` |
| OPEN-10 | Almacenamiento de copias en proveedor distinto del host | `REQ-BKP` |

---

## Problemas abiertos

| ID | Descripción | Severidad |
|----|-------------|-----------|
| P-02 | `npm create vue@latest -- --typescript --router ...` no respeta sus propios flags: sigue pidiendo el nombre del paquete de forma interactiva. Intentar automatizarlo con `yes "" \| npm create vue@latest ...` cuelga el proceso al 100% de CPU en vez de fallar limpio. Usar `npm create vite@latest <dir> -- --template vue-ts` y añadir router/Tailwind/shadcn-vue/Vitest/Playwright/ESLint a mano (lo que se hizo en 0.5) evita el problema. No usar `create-vue` en `apps/web` sin resolver esto primero. | Baja |
| [#6](https://github.com/pirexia/plataforma-educativa/issues/6) | `TenantContext::runAsPlatform()` sin control de autorización ni auditoría — cualquier código con acceso al contenedor puede cruzar tenants sin dejar rastro. Retomar cuando exista el sistema de permisos (1.5). | Media |
| [#7](https://github.com/pirexia/plataforma-educativa/issues/7) | La caché de resolución de tenant (`ResolveTenant`, 60s) no se invalida al suspender un tenant: sigue entrando hasta que expira. Retomar en REQ-BO-001 (backoffice, suspensión de tenants) u 0.8. | Baja |
| [#8](https://github.com/pirexia/plataforma-educativa/issues/8) | La cookie de sesión "host-only" es el valor por defecto de Laravel (`SESSION_DOMAIN` sin fijar), no algo reforzado activamente en el código — anotar para no darlo por garantizado si algo cambia esa config sin querer. Revisar al implementar `REQ-AUTH` (1.2). | Baja |
| [#18](https://github.com/pirexia/plataforma-educativa/issues/18) | `password_reset_tokens` ya tiene `tenant_id`+RLS (0.8), pero falta un `PasswordBrokerRepository` propio que filtre por tenant en la aplicación — hoy la única barrera es RLS, no framework+RLS. No explotable (el flujo de recuperación no existe hasta 1.2). Resolver ahí, no en 0.9. | Media |
| [#27](https://github.com/pirexia/plataforma-educativa/issues/27) | `Tenant` sin ningún registro de auditoría (alta, suspensión, cambio de `slug`, baja) hasta que exista `admin_action_logs`. Ver `ADR-036`. No explotable hoy (nadie usa `runAsPlatform()` sobre un modelo `Auditable`). Resolver en 1.6 (`REQ-BO`). | Media |

---

## Siguiente paso concreto

1. **Confirmar si el PR de `feature/REQ-0.9-auditoria-i18n` se fusionó a `develop`** antes de arrancar nada más — la sesión anterior lo abrió y se cortó justo después por límite de cuota, sin confirmar el resultado de los checks de CI. Si no se fusionó: comprobar los checks (`gh pr checks` puede fallar con 403 por el permiso de "Checks" que le falta al PAT, ver `SYSADMIN.md` §4 punto 4 — en ese caso `gh pr merge --squash --delete-branch` sirve como comprobación indirecta: si branch protection lo bloquea, el comando falla y lo dice). Tras fusionar: sincronizar `develop`, borrar la rama local, y verificar que el *worktree* `.claude/worktrees/agent-a90a647c37bca30c5` (si sigue existiendo) se puede limpiar.
2. **Después, empezar 0.10 · Decidir alojamiento del piloto** (`OPEN-11`, bloqueante del hito H0) y evaluar si continuar con 0.10b/0.10c/0.10d (dominio+DNS, correo transaccional, destino de copias) — son en su mayoría **decisiones de negocio, no de código**: necesitan que el usuario elija proveedor/contrate algo, no que la sesión implemente. Al arrancar, preguntar al usuario si tiene ya alguna de estas decisiones tomadas antes de proponer nada.
3. **Cerrado (2026-08-18)**: branch protection en `develop` con los ocho checks (usuario, vía UI), Dependabot alerts activado (usuario). PR onboarding de Renovate [#10](https://github.com/pirexia/plataforma-educativa/pull/10) mezclada — pero Renovate sobrescribió `renovate.json` en `main` con la config genérica (issue [#13](https://github.com/pirexia/plataforma-educativa/issues/13), cerrado): solucionado cambiando la **rama por defecto del repo de `main` a `develop`** (usuario, vía UI; también concedió permiso "Administration" al PAT de `github`), que es donde vive el `renovate.json` bueno (timezone Madrid, agrupación por app, `vulnerabilityAlerts`). Verificado: `gh api repos/.../--jq '.default_branch'` → `develop`, contenido de `renovate.json` en `develop` correcto. El permiso "Checks" para el PAT sigue sin existir — GitHub lo desactivó para *fine-grained PAT* (ver discusión oficial), no es un problema de nuestra configuración; asumido, no bloquea nada (branch protection hace cumplir los checks del lado de GitHub aunque yo no pueda leer su estado).
4. Decidir el motor de renderizado PDF (o posponerlo explícitamente a 1.17) antes de que haga falta en 1.17.
5. Los issues [#6](https://github.com/pirexia/plataforma-educativa/issues/6)/[#7](https://github.com/pirexia/plataforma-educativa/issues/7)/[#8](https://github.com/pirexia/plataforma-educativa/issues/8)/[#18](https://github.com/pirexia/plataforma-educativa/issues/18)/[#27](https://github.com/pirexia/plataforma-educativa/issues/27) (ver `Problemas abiertos`) no bloquean nada del trabajo actual; están correctamente diferidos a 1.2/1.5/1.6/REQ-BO-001, que es cuando existe el código que los necesita.

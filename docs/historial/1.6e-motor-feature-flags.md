# 1.6e · `REQ-BO-005`: motor de *feature flags*

**CERRADO Y MEZCLADO** (2026-09-22). Último de los cinco sub-pasos de `REQ-BO`; **los cinco (`1.6`-`1.6e`) quedan cerrados**. PR [#240](https://github.com/pirexia/plataforma-educativa/pull/240) (*squash*, commit `da1b84f`), 13/13 *checks* de CI en verde. Rama local y remota borradas; *worktree* del `implementer` (`agent-ab76888270a007ef0`) verificado sin contenido único (diff vacío contra lo ya mezclado) y eliminado. Implementado por `implementer` (`isolation: worktree`, quedó en rama propia por necesidad estructural de git, fusionado por *fast-forward* sin conflicto).

Verificado por la sesión orquestadora contra el contenedor de referencia (no el *sandbox* del `implementer`): **741/741 Pest, Pint limpio (843 ficheros), Larastan 0 errores** — confirma que los 74 fallos de `REQ-AUTH` vistos por el `implementer` eran el mismo problema de entorno de `1.6d` (desincronización de `storage/framework/testing/saml-fake-idp/` entre *worktree* y contenedor), no una regresión.

Revisión independiente (`db-reviewer`/`security-reviewer`/`doc-reviewer` en paralelo): 1 Alto + 5 Media + 1 Baja, corregidos en la misma sesión salvo dos dejados como decisión abierta a propósito. Detalle completo en `CHANGELOG.md` y `funcional.md §15.5`/`§15.5.1`.

## Corregidos

`GET /api/v1/feature-flags` sin autenticación (Alto, `INV-002` — el docblock afirmaba un *guard* que no existía); falta de `CHECK` en `feature_flag_rules.reason` (Media, migración aditiva nueva); `affected_tenant_id` (bigint interno) filtrado en la respuesta HTTP de `PUT .../rules` (Media, `ADR-029`); recuento erróneo de migraciones en `funcional.md §15.5` (Media, "dos" → "tres").

## Dejados abiertos, no corregidos a propósito

`OPEN-BO-27` (nueva — `rollout_unit: 'user'` deja vista previa/impacto ciegos a `percentage`, decisión de alcance); issue [#238](https://github.com/pirexia/plataforma-educativa/issues/238) (registro de `ADR-051` no puede cubrir `permissions.code` por el mismo mecanismo que `modules.code` — ambigüedad real de `ADR-051 §5.3`, el registro es estructuralmente de parámetros de ruta y `permissions.code` nunca lo es; comentado en el issue, no decidido); issue [#239](https://github.com/pirexia/plataforma-educativa/issues/239) (Baja, comentario obsoleto en `routes.php`, política `CLAUDE.md §5`).

## Worktree huérfano sin relación

`agent-a3c6dc392b967572e` (c476d66) quedó como *worktree* huérfano sin relación con esta sesión ni con `1.6e` — no tocado, pendiente de verificar en otra sesión antes de decidir si se limpia. (Nota vigente al 2026-09-24: sigue sin tocar.)

## Sesión de mantenimiento posterior (2026-09-22, cerrada y mezclada)

PR [#246](https://github.com/pirexia/plataforma-educativa/pull/246) (*squash*, commit `601a0e5`), 13/13 *checks* de CI en verde, rama borrada. Cerró siete issues de deuda técnica diferidos tras `1.6e`: `#211`+`#182` (`lockForUpdate()` ausente, TOCTOU — `CheckGracePeriodsCommand`, `PlatformAdminInvitationRedemptionService`, `EloquentInvitationRedeemer`), `#186`+`#201` (convención `NOT VALID`+`VALIDATE CONSTRAINT` nueva en la skill `migracion-segura`, sin tocar migraciones ya aplicadas), `#236` (`CACHE_STORE=redis` en `compose.yaml`/`infra/quadlet/plataforma.env.example`/`infra/compose/compose.prodlike.yaml` + `ext-redis` en `composer.json`), y `#243`+`#244` (dos defectos reales que `db-reviewer` encontró en el propio fix de `#211` durante la revisión de esta misma sesión — recheck incompleto y test que no ejercitaba el código que decía probar —, corregidos y verificados en ambos sentidos: el test nuevo pasa con el fix y falla sin él, 8/8 ejecuciones sin dependencia de orden).

Revisión independiente `db-reviewer`+`security-reviewer` en paralelo, sin hallazgos sin corregir salvo `#245` (cuarta repetición del `CHECK` sin `NOT VALID`, ya en `develop` desde `1.6e`, no corregible retroactivamente — issue abierto, informativo). 742/742 Pest en verde, Pint y Larastan limpios. **Lección de proceso confirmada de nuevo**: una revisión independiente sobre el propio trabajo de la sesión, no solo sobre el de un `implementer` subagente, sigue encontrando defectos reales — no hay atajo por "es solo una sesión de mantenimiento corta".

### Punto de situación de issues abiertos (triage 2026-09-22)

Triage completo de los ~70 issues abiertos entonces, por un subagente, con verificación cruzada de la sesión orquestadora sobre los de mayor severidad.

**Cerrados 8, ya resueltos por trabajo anterior y sin código nuevo que tocar**: [#135](https://github.com/pirexia/plataforma-educativa/issues/135), [#136](https://github.com/pirexia/plataforma-educativa/issues/136) (CI/`.gitignore`, verificados en vivo contra el fichero real), [#150](https://github.com/pirexia/plataforma-educativa/issues/150), [#151](https://github.com/pirexia/plataforma-educativa/issues/151) (causa raíz cerrada por el `chore/` de alcance de subagentes de 2026-09-04), [#193](https://github.com/pirexia/plataforma-educativa/issues/193) (norma ya en `CLAUDE.md §2`), [#192](https://github.com/pirexia/plataforma-educativa/issues/192)/[#179](https://github.com/pirexia/plataforma-educativa/issues/179) (SAML en rojo por *worktree* desincronizado del contenedor — tercera confirmación independiente del mismo mecanismo ya documentado en `1.6d`/`1.6e`), [#188](https://github.com/pirexia/plataforma-educativa/issues/188) (la norma que pedía ya existe y se aplica).

**Verificado que sigue vigente, no tocado**: [#144](https://github.com/pirexia/plataforma-educativa/issues/144) (Dependabot `decode-uri-component`, la cadena `vite→stylus` sigue en `apps/web/package-lock.json`).

**Excluido del triage**: [#12](https://github.com/pirexia/plataforma-educativa/issues/12) (panel de Renovate, no es un bug).

**Quedaron sin tocar por decisión del usuario** (2026-09-22: solo limpieza administrativa esa sesión, no código): `lockForUpdate()` ausente ([#211](https://github.com/pirexia/plataforma-educativa/issues/211)+[#182](https://github.com/pirexia/plataforma-educativa/issues/182)) y `CHECK` sin `NOT VALID` ([#186](https://github.com/pirexia/plataforma-educativa/issues/186)+[#201](https://github.com/pirexia/plataforma-educativa/issues/201)) — y [#236](https://github.com/pirexia/plataforma-educativa/issues/236); los tres se resolvieron en la sesión de mantenimiento descrita arriba, el mismo día.

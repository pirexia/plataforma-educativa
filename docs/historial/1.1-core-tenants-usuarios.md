# Historial · 1.1 · `REQ-CORE`: tenants y usuarios

> Archivado desde `memory.md` el 2026-08-25 para hacer sitio a 1.2. Cerrado y mezclado 2026-08-21/22, PR [#56](https://github.com/pirexia/plataforma-educativa/pull/56) (*squash* a `develop`, commit `d32e4e9`).

## Issue #55, resuelto por la orquestadora directamente
`TenantMigrationTest.php` reescrito (aserción y comentario invertidos para probar el comportamiento actual y correcto — `tenantTable()` añade la FK porque `users.tenant_id` siempre existe con el set completo de migraciones), verificado eliminando la tabla `tenant_migration_probes` de la BD compartida y dejando que se recreara desde cero. Commit `be84ce6`. 183/183 tests, PHPStan 0 errores, Pint limpio.

## Issue #51, resuelto (rama `fix/REQ-CORE-larastan-schema-scan`, dos commits)
`barryvdh/laravel-ide-helper` (`^3.7`, MIT, dev-only). `phpstan analyse`: **234 → 34 → 0 errores**.
- `33d98ea`: dependencia + `_ide_helper_models.php` generado (introspección contra BD real, PostgreSQL desechable propio) + `@mixin IdeHelperX` a mano en los 15 modelos reales. **Hallazgo importante**: `scanFiles` en `phpstan.neon` con el fichero generado, por sí solo, **no hace nada** — el `@mixin` en cada modelo es imprescindible. `_ide_helper_models.php` se **commitea** (no se regenera en CI, sin PostgreSQL); regenerar tras cada migración nueva, instrucciones en `CONTRIBUTING.md` §3.1.
- `604a494`: de los 34 supervivientes, 32 eran reales y triviales; los 2 restantes eran el fallo preexistente de `TenantMigrationTest.php` (issue #55).
- Verificado: 182/183 tests, `pint --test` limpio en los 33 ficheros. Issue #52: 9 ficheros preexistentes con `pint --test` roto, no 4.

## 1.1: primeros diez commits (sesión anterior a esta, resumido)
Formato de error `problem+json`, migraciones+modelos+`CoreServiceProvider`, `PermissionResolver` provisional, `tenant:provision-defaults`, endpoints de configuración/usuarios/`me`/invitaciones/roles-permisos-módulos/auditoría+exportación. Issues: #48 (16 vs 17 roles, cerrado), #49 (Alta, corregido).

## 1.1: cinco commits de la sesión de cierre
- `46d982d` **activos de marca**: `PUT`/`DELETE /tenant/settings/assets/{kind}`. Tipo real por contenido (`finfo`), rechazo si el tipo real no coincide con el declarado (`RN-CORE-18`), saneado de SVG con `DOMDocument`. `CA-CORE-005/006`.
- `e7d6f74` **importación masiva + idempotencia + purgas + interfaces + eventos**: `UserImportCsvReader`/`UserImportRowValidator`, jobs `ValidateUserImport`/`ExecuteUserImport` (cola `core-imports`), `RequireIdempotencyKey` (contrato `ADR-038 §8`). Cinco purgas orquestadas por `core:purge-maintenance`. Interfaces `BulkUserImporter`/`UserDirectory`. Eventos `TenantSettingsUpdated`/`UserImportCompleted`. `CA-CORE-030` a `035`.
  - Issue #50, corregido en el mismo commit: `IdempotencyKey` trasladado de `App\Modules\Core\Domain\Models` a `App\Models`.
- `e3c1c9c` huecos de test: `CA-CORE-041`, `071`/`072`/`073`, `075`.
- `f38e333` **OpenAPI completo**: `components.yaml` + `paths/core.yaml` (33 operaciones), paridad 1:1 con `route:list` verificada a mano. Corrige "17 roles" → 16 (issue #48).
- `2914358` **cliente TS**: `apps/web/src/modules/core/{api,types,locales}/`. Nada en `views/`/`components/` (1.1 sin pantallas, `OPEN-CORE-02`).
  - Issue #51, no corregido en ese commit (fuera de alcance, requería decisión): `phpstan analyse` con 228 errores `property.notFound` — resuelto después, ver arriba.
  - Issue #52, no corregido (código ajeno): 4 ficheros preexistentes sin `pint --test`.
  - Los 76 `CA-CORE-*` tienen test que los referencia.
- `4a7ece3` **hallazgos de la revisión independiente** (`security-reviewer`/`doc-reviewer`, sobre `eb8e10a..HEAD`):
  - `security-reviewer`: sin hallazgos Crítico/Alto. Confirmó que el aislamiento de tenant en los jobs nuevos lo da el mecanismo de framework de `TenancyServiceProvider`. Issue #53 (Media, corregido en el mismo commit: faltaban tests de aislamiento cruzado para `/user-imports/*` y `assets/{kind}`) e issue #54 (Baja, no corregido a propósito: regex de `SvgSanitizer` podría truncarse ante un DTD con `>` interno, no explotable hoy).
  - `doc-reviewer`: 5 hallazgos Media, todos corregidos (ejemplo `"total": 17`, `CA-CORE-041` código de estado, `404` de `assets/{kind}` sin documentar, tabla de eventos, envoltura de `GET /user-imports`).
- 183/183 tests de `apps/api` en verde; frontend `vue-tsc -b`/`eslint`/`lint:i18n`/`vitest run`/`npm run build` en verde.

**Nota honesta pendiente sin resolver**: no se pudo verificar el resultado de `ci-api.yml`/`ci-web.yml` sobre el PR #56 antes de mezclar — el token de `gh` no tenía permiso para leer *check runs* (403). Se mezcló confiando en la verificación local exhaustiva.
